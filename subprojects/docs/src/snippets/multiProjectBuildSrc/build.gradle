apply plugin: "plugina"
apply plugin: "pluginb"

task showPlugins {
    def pluginClasses = project.plugins.collect { it.class }
    doLast {
        pluginClasses.each {
            println(it.name)
        }
    }
}
